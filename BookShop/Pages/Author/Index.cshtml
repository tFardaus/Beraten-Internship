@page
@model BookShop.Pages.Author.IndexModel
@{
    ViewData["Title"] = "Authors";
}

<h1>Authors List</h1>
<hr />
<div class="row mb-3">
    <div class="col-md-6">
        <form method="get">
            <div class="input-group">
                <input type="text" name="searchTerm" value="@Model.SearchTerm" class="form-control" placeholder="Search by name..." />
                <button type="submit" class="btn btn-primary">Search</button>
                <a asp-page="./Index" class="btn btn-secondary">Clear</a>
            </div>
        </form>
    </div>
    <div class="col-md-6 text-end">
        <a asp-page="Create" class="btn btn-success">Create</a>
    </div>
</div>
<table class="table table-responsive table-striped table-hover align-middle">
    <thead>
        <tr class="text-center">
            <th>Name</th>
            <th>Biography</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var author in Model.Authors)
        {
            <tr class="text-center">
                <td>
                    @if (Model.AuthorJsonData.ContainsKey(author.AuthorId))
                    {
                        <span>@Model.AuthorJsonData[author.AuthorId].Name</span>
                    }
                    else
                    {
                        <span class="text-muted">No JSON Data</span>
                    }
                </td>
                <td>
                    @if (Model.AuthorJsonData.ContainsKey(author.AuthorId))
                    {
                        <span>@Model.AuthorJsonData[author.AuthorId].Biography</span>
                    }
                    else
                    {
                        <span class="text-muted">No JSON Data</span>
                    }
                </td>
                <td>
                    <a asp-page="Details" asp-route-id="@author.AuthorId" class="btn btn-sm btn-success">Details</a>
                    <a asp-page="Edit" asp-route-id="@author.AuthorId" class="btn btn-sm btn-primary">Edit</a>
                    <a asp-page="Delete" asp-route-id="@author.AuthorId" class="btn btn-sm btn-danger">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
